apiVersion: 1

providers:
  - name: LibreChat
    orgId: 1
    folder: LibreChat
    type: file
    disableDeletion: false
    updateIntervalSeconds: 60
    options:
      path: /etc/grafana/provisioning/dashboards/json
